apiVersion: v1
kind: Service
metadata:
  name: {{ include "identity-access-management.fullname" . }}
  labels:
    {{- include "identity-access-management.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "identity-access-management.selectorLabels" . | nindent 4 }}
