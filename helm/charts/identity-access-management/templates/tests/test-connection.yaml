apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "identity-access-management.fullname" . }}-test-connection"
  labels:
    {{- include "identity-access-management.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "identity-access-management.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
