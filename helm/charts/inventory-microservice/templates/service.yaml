apiVersion: v1
kind: Service
metadata:
  name: {{ include "inventory-microservice.fullname" . }}
  labels:
    {{- include "inventory-microservice.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "inventory-microservice.selectorLabels" . | nindent 4 }}
