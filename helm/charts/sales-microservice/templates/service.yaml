apiVersion: v1
kind: Service
metadata:
  name: {{ include "sales-microservice.fullname" . }}
  labels:
    {{- include "sales-microservice.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "sales-microservice.selectorLabels" . | nindent 4 }}
