{{- if .Values.mysql.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ printf "%s-rp-sales-sql-svc" .Release.Name }}
  namespace: {{ include "sales.namespace" . }}
  labels:
    {{- include "sales.labels" . | nindent 4 }}
    app: {{ printf "%s-rp-sales-sql-deploy" .Release.Name }}
spec:
  selector:
    app: {{ printf "%s-rp-sales-sql-deploy" .Release.Name }}
  ports:
    - protocol: TCP
      port: 3306
      targetPort: 3306
      nodePort: {{ .Values.mysql.nodePort }}
  type: NodePort
{{- end }}